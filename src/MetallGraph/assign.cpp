#include <metall_graph.hpp>

namespace metalldata {
metall_graph::return_code assign(
  std::string_view &sel, const metall_graph::data_types &val,
  const metall_graph::where_clause &where_clause) {
  if (is_node_selector(sel))
    if (!) }
}  // namespace metalldata